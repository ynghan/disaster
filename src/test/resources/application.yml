server:
  address: localhost
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shelter?characterEncoding=UTF-8&characterSetResults=UTF-8
    username: root
    password: dudgks56!!

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

kakao:
  api:
    key: bb490f917dcba516e3e94c6228806224


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace